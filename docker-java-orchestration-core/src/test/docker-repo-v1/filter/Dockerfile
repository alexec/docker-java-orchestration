FROM ${foo}/${foo}:${foo}
MAINTAINER ${foo}
EXPOSE ${foo}
USER ${foo}
ENV ${foo} ${foo}
RUN ${foo}
CMD ["${foo}"]
ONBUILD ${foo}
ENTRYPOINT ${foo}
ADD ${testFile.name} /
COPY ${testFile.name} /
VOLUME ${foo}
WORKDIR ${foo}
