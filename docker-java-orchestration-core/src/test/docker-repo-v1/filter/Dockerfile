FROM ${foo.bar}/${foo.bar}:${foo.bar}
MAINTAINER ${foo.bar}
EXPOSE ${foo.bar}
USER ${foo.bar}
ENV ${foo.bar} ${foo.bar}
RUN ${foo.bar}
CMD ["${foo.bar}"]
ONBUILD ${foo.bar}
ENTRYPOINT ${foo.bar}
ADD ${testFile.name} /
COPY ${testFile.name} /
VOLUME ${foo.bar}
WORKDIR ${foo.bar}
